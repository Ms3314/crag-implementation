# Core LangChain / LangGraph stack
langchain==1.4.0
langchain-core==1.6.3
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langgraph==1.2.11

# Model integrations
langchain-ollama==1.1.0          # ChatOllama (LLM) + optional Ollama embeddings
langchain-huggingface==1.2.2     # HuggingFace serverless embeddings
langchain-groq==1.1.3            # optional Groq LLM (llm2)
langchain-openai==1.6.2          # optional OpenAI embeddings (EMBED_BACKEND=openai)

# Vector store
faiss-cpu==1.15.0

# Document loading / splitting
pypdf==6.18.1

# Reranker (local cross-encoder)
sentence-transformers==6.0.1
torch==2.14.0
transformers==5.17.0

# API clients / utilities
huggingface-hub==1.31.0
ollama==0.6.2
groq==0.37.1
openai==3.13.0
python-dotenv==1.2.3

# IMPORTANT: httpx2 (pulled in by openai) calls
# brotlicffi.Decompressor.decompress(..., output_buffer_limit=...). Versions of
# brotlicffi older than 1.2.0.2 don't support that kwarg and every OpenAI/HF call
# fails with a misleading "APIConnectionError: Connection error".
brotlicffi>=1.2.0.2
langchain-tavily